# Next Step

Read these files first:

- `README.md`
- `../../README.md`
- `../../PROJECT_CONTEXT.md`

Current starting file:

- `elitech_and_teh_without_images.xlsx`

What this file means:

- it already contains only rows from main sections `Elitech` and `TEH`
- it contains only products where both image columns are empty
- it contains only rows with a non-empty article

What to do next:

- use `elitech_and_teh_without_images.xlsx` as the honest starting slice
- create one separate iteration directory per source
- download images into per-article folders
- after any manual review, rebuild the remainder from actual surviving folders
- keep the workflow consistent with the Makita project structure

Expected outcome:

- a truthful sequence of source iterations
- a final merged delivery with per-article folders and import-ready Excel
- updated local documentation when the context changes
