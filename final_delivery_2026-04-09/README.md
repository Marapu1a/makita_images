# Final Delivery

- `import_images/` contains the final per-article folders.
- Real images are copied from successful iteration outputs.
- Remaining articles are closed with `placeholder.webp` copied as `preview.webp`.
- `final_report.xlsx` contains the итоговый статус for all processed articles.
